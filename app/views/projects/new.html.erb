<h1><%= t("projects.new") %></h1>

<%= form_with model: @project, url: projects_path do |form| %>
  <% if @project.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@project.errors.count, "error") %> prohibited this project from being saved:</h2>

      <ul>
        <% @project.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name, t('projects.form.name') %>
    <%= form.text_field :name, id: :project_name, required: true %>
  </div>

  <div class="actions">
    <%= form.button :submit %>
  
  </div>
<% end %>
  <div>
    <p><%= link_to "Quay lại", projects_path %></p>

  </div>

