<h1>Chỉnh sửa Checkin/out</h1>

<%= form_with(model: [@employee, @attendance], local: true) do |form| %>
  
  <div class="form-group">
    <%= form.label :start_time, 'Giờ bắt đầu' %>
    <%= form.datetime_local_field :start_time, class: "form-control" %>
  </div>

  <div class="form-group">
    <%= form.label :end_time, 'Giờ kết thúc' %>
    <%= form.datetime_local_field :end_time, class: "form-control" %>
  </div>


  <%= form.submit 'Cập nhật', class: "btn btn-primary" %>
<% end %>

<%= link_to 'Quay lại', employee_path(@employee), class: 'btn btn-secondary' %>
